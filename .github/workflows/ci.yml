name: CI

on:
  push:
    branches: [main, master, chapter01, chapter02, chapter03, chapter04, chapter05, chapter06, chapter07, '07']
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  chapter:
    name: ${{ matrix.chapter }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        chapter:
          - chapter01
          - chapter02
          - chapter03
          - chapter04
          - chapter05
          - chapter06
          - chapter07
        node-version: [20]

    defaults:
      run:
        working-directory: ${{ matrix.chapter }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: ${{ matrix.chapter }}/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test with coverage
        if: matrix.chapter == 'chapter07'
        run: npm run test:coverage

      - name: Upload coverage report
        if: matrix.chapter == 'chapter07' && always()
        uses: actions/upload-artifact@v4
        with:
          name: chapter07-coverage
          path: |
            chapter07/coverage/lcov.info
            chapter07/coverage/coverage-summary.json
            chapter07/coverage/lcov-report
          if-no-files-found: warn

      - name: Ensure secrets and local DB are not tracked
        working-directory: ${{ github.workspace }}
        run: |
          set -euo pipefail
          for f in \
            "${{ matrix.chapter }}/.env" \
            "${{ matrix.chapter }}/data/gateway.db"
          do
            if git ls-files --error-unmatch "$f" 2>/dev/null; then
              echo "::error::$f must never be committed"
              exit 1
            fi
            echo "OK: $f is not tracked"
          done
